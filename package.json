{
  "name": "vaunt-vault-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --cwd apps/api dev & bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build"
  }
}
